{
  "name": "athishta-gold-company",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "node scripts/ensure-cloudflare-binding.mjs && npm --workspace frontend run build",
    "dev": "npm --workspace frontend run dev",
    "lint": "npm --workspace frontend run lint"
  }
}
