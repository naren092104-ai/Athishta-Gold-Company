{
  "name": "athishta-gold-company",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm --workspace frontend run build",
    "deploy": "npx wrangler pages deploy dist --project-name athishta-gold-company",
    "dev": "npm --workspace frontend run dev",
    "lint": "npm --workspace frontend run lint"
  }
}
